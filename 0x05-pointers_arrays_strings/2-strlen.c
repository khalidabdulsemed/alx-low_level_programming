#include "main.h"

/**
 *  * _strlen - length of a string
 *   * @s: input char
 *    * Return: length of a string
 */

int _strlen(char *s)
{
	int len = 0;
	
	while (len !='\0')
	{
		s++;
		l++;
	}
	return (len);
}
