#include "main.h"
#include <stdio.h>

/**
 *  * _strlen - length of a string
 *   * @s: input char
 *    * Return: length of a string
 */

int _strlen(char *s)
}
	int len;
	len = strlen(*str);
	
	return (len);
}


int main(void)
{
	    char *str;
	        int len;

		    str = "My first strlen!";
		        len = _strlen(str);
			    printf("%d\n", len);
			        return (0);
}
